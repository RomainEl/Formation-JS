$(function(){

    // On récupère les variables
    var nom = $('#nom');
    var prenom = $('#nom');
    var email = $('#nom');
    var password = $('#nom');
    var submit = $('#submit');

    // Vérification des champs
    nom.on('input', function(e){
        e
    })
});